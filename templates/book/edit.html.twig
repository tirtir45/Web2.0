<h1>Edit Book</h1>

{{form_start(form)}}
        {{form_row(form.title)}}
        {{form_row(form.publicationDate)}}
        {{form_row(form.category)}}
        {{form_row(form.enabled)}}
        {{form_row(form.author_b)}}

<button type="submit">Save</button>

{{form_end(form)}}