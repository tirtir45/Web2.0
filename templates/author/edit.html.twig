<h1>Edit Author</h1>

{{form_start(form)}}
        {{form_row(form.username)}}
        {{form_row(form.email)}}
        {{form_row(form.nbBooks)}}

<button type="submit">Save</button>

{{form_end(form)}}